# Dashboard Home Figma Source

Record the canonical Figma link for the Dashboard Home entry here before the item advances from Ready for Review to Preview.

Keep the link in sync with the `figma_link` field in `entry.yaml`.

# Landing Page Figma Source

Add the canonical Figma link for the Landing Page entry before this archetype moves from Ready for Review to Preview.

The `figma_link` field in `entry.yaml` must match the final source link recorded here.

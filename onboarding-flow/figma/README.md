# Onboarding Flow Figma Source

Add the canonical Figma link for the Onboarding Flow entry here before it advances from Ready for Review to Preview.

Mirror the same link in the `figma_link` field in `entry.yaml`.
